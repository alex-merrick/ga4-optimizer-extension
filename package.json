{
  "name": "ga4-optimizer-extension",
  "version": "1.0.0",
  "description": "Website for GA4 Optimizer Extension",
  "main": "index.js",
  "scripts": {
    "start": "npx @11ty/eleventy --serve",
    "build": "npx @11ty/eleventy",
    "clean": "node scripts/clean.mjs",
    "verify": "node scripts/check-nav.mjs",
    "check": "npm run clean && npm run build && npm run verify"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^3.1.5",
    "@netlify/functions": "^5.2.0",
    "clean-css": "^5.3.3",
    "markdown-it": "^14.1.0",
    "sharp": "^0.35.3",
    "terser": "^5.46.2"
  }
}
