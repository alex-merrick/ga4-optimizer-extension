---
layout: "layouts/base.njk"
title: "Blog | GA4 Optimizer"
description: "Tips, tricks, and guides for making Google Analytics 4 easier to use and more powerful."
permalink: "blog/index.html"
---
<main class="container">
    <div class="main-content content-page">
        <h1>GA4 Optimizer Blog</h1>
        <p>Actionable advice on solving common GA4 frustrations and getting more from your data.</p>
        
        <hr style="border-color: var(--border-color); margin-bottom: 40px;">

        <ul style="list-style: none; padding-left: 0;">
            {%- for post in collections.posts -%}
                <li class="blog-post-item" style="margin-bottom: 50px; padding-bottom: 30px; border-bottom: 1px solid var(--border-color);">
                    <!-- Use Flexbox for side-by-side layout -->
                    <div class="blog-post-layout" style="display: flex; gap: 30px; align-items: flex-start;">
                        
                        <!-- Thumbnail Column -->
                        {%- if post.data.thumbnail -%}
                        <div style="flex-shrink: 0; width: 200px;">
                            <a href="{{ post.url }}">
                                <img src="{{ post.data.thumbnail }}" alt="{{ post.data.title }}" style="width: 100%; height: 120px; object-fit: cover; border-radius: 8px; display: block;">
                            </a>
                        </div>
                        {%- endif -%}

                        <!-- Text Content Column -->
                        <div style="flex-grow: 1;">
                            <h2 style="font-size: 1.8rem; margin-top:0; margin-bottom: 5px;"><a href="{{ post.url }}">{{ post.data.title }}</a></h2>
                            <p style="color: var(--text-light); font-style: italic; margin-top: 0;">Published on {{ post.date.toDateString() }}</p>
                            <p>{{ post.data.description }}</p>
                            <p><a href="{{ post.url }}" style="font-weight: 600;">Read more...</a></p>
                        </div>

                    </div>
                </li>
            {%- endfor -%}
        </ul>
    </div>
</main>