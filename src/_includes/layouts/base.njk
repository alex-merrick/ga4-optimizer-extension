<!DOCTYPE html>
<html lang="en">
<head>
    <!-- Google Tag Manager -->
<script>(function(w,d,s,l,i){w[l]=w[l]||[];w[l].push({'gtm.start':
new Date().getTime(),event:'gtm.js'});var f=d.getElementsByTagName(s)[0],
j=d.createElement(s),dl=l!='dataLayer'?'&l='+l:'';j.async=true;j.src=
'https://www.googletagmanager.com/gtm.js?id='+i+dl;f.parentNode.insertBefore(j,f);
})(window,document,'script','dataLayer','GTM-KNVNWPQ6');</script>
<!-- End Google Tag Manager -->
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <meta name="description" content="{{ description }}">
    <link rel="stylesheet" href="/style.css">
    <link rel="icon" href="/icons/icon128-min.png" type="image/png">
    <link rel="canonical" href="https://www.gaoptimizer.com{{ page.url }}" />

    <!-- Schema will be injected here if provided -->
    {% if schema %}
    <script type="application/ld+json">
    {{ schema | safe }}
    </script>
    {% endif %}

    {% if page.url == '/documentation.html' %}
    <!-- Page-specific styles for documentation layout -->
    <style>
        .docs-layout { display: flex; gap: 40px; margin-top: 20px; }
        #docs-sidebar { width: 240px; flex-shrink: 0; position: sticky; top: 100px; align-self: flex-start; border: 1px solid var(--border-color); background-color: var(--bg-light); border-radius: 6px; }
        .sidebar-title { font-size: 1.1em; font-weight: 600; color: var(--brand-purple-dark); margin: 0; padding: 12px 15px; border-bottom: 1px solid var(--border-color); }
        .sidebar-content { padding: 8px 12px; }
        #docs-sidebar ul { list-style-type: none; padding-left: 0; margin: 0; }
        #docs-sidebar ul li a { display: block; padding: 3px 8px; font-size: 0.9em; color: var(--text-light); text-decoration: none; border-radius: 4px; transition: background-color 0.2s ease, color 0.2s ease; font-weight: 500; }
        #docs-sidebar ul li a:hover { background-color: #e9e6ed; color: var(--brand-purple); }
        #docs-sidebar ul li a.active { background-color: var(--brand-purple); color: var(--bg-white); font-weight: 600; }
        #docs-sidebar ul ul { padding-left: 15px; margin-top: 4px; margin-bottom: 4px; }
        #docs-sidebar ul ul li a { font-size: 0.9em; padding: 3px 8px; }
        .docs-content { flex-grow: 1; min-width: 0; font-size: 1.05em; }
        .docs-content section { border-top: 1px solid var(--border-color); padding-top: 20px; margin-top: 20px; }
        .docs-content section:first-child { border-top: none; margin-top: 0; padding-top: 0; }
        .note, .requirement-note { padding: 14px 20px; margin: 18px 0; font-size: 1em; border-radius: 4px; border-left: 4px solid; }
        .note { background-color: #f0e6f7; border-color: var(--brand-purple); }
        .requirement-note { background-color: #fdf1e6; border-color: #F9AB00; }
        .note strong { color: var(--brand-purple-dark); }
        .requirement-note strong { color: #532e01; }
        .settings-gear-inline { width: 16px; height: 16px; vertical-align: -0.2em; object-fit: contain; margin: 0 2px; filter: grayscale(20%) opacity(85%); }
        @media (max-width: 900px) { .docs-layout { flex-direction: column; gap: 0; } #docs-sidebar { width: 100%; position: sticky; top: 70px; height: auto; z-index: 900; overflow-y: visible; margin-bottom: 20px; } .sidebar-title { cursor: pointer; position: relative; background-color: var(--bg-light); } .sidebar-title::after { content: '▾'; position: absolute; right: 20px; top: 50%; transform: translateY(-50%); font-size: 1.2em; transition: transform 0.3s ease; } #docs-sidebar.is-open .sidebar-title::after { transform: translateY(-50%) rotate(180deg); } .sidebar-content { max-height: 0; overflow: hidden; padding: 0 15px; margin: 0; transition: max-height 0.4s ease-out, padding 0.4s ease-out; background-color: var(--bg-light); border-top: 1px solid var(--border-color); } #docs-sidebar.is-open .sidebar-title { border-bottom-left-radius: 0; border-bottom-right-radius: 0; } #docs-sidebar.is-open .sidebar-content { padding: 15px; } #docs-sidebar ul { margin: 0; } }
    </style>
    {% endif %}

</head>
<body>
<!-- Google Tag Manager (noscript) -->
<noscript><iframe src="https://www.googletagmanager.com/ns.html?id=GTM-KNVNWPQ6"
height="0" width="0" style="display:none;visibility:hidden"></iframe></noscript>
<!-- End Google Tag Manager (noscript) -->
    <header class="site-header">
        <div class="nav-container">
            <a href="/" class="brand-logo">
                <img src="/icons/icon128-min.png" alt="GA4 Optimizer Logo">
                <span class="brand-name">GA4 Optimizer</span>
            </a>
            <nav class="main-nav">
                <ul>
                    <li><a href="/" {% if page.url == '/' %}class="active" aria-current="page"{% endif %}>Home</a></li>
                    <li><a href="/blog/" {% if page.url.startsWith('/blog') %}class="active" aria-current="page"{% endif %}>Blog</a></li>
                    <li><a href="/documentation.html" {% if page.url == '/documentation.html' %}class="active" aria-current="page"{% endif %}>Documentation</a></li>
                    <li><a href="/privacy.html" {% if page.url == '/privacy.html' %}class="active" aria-current="page"{% endif %}>Privacy</a></li>
                    <li><a href="https://chromewebstore.google.com/detail/ga4-optimizer/hlldjkhoepkephgaeifgbelgchncfnjj" class="cta-button" target="_blank" rel="noopener noreferrer">Add to Chrome</a></li>
                </ul>
            </nav>
            <button class="nav-toggle" aria-label="Toggle navigation">
                <span></span>
            </button>
        </div>
    </header>

    {{ content | safe }}

    <!-- Video Modal Structure -->
    <div id="videoModal" class="video-modal">
        <div class="modal-content">
            <button class="modal-close-button">×</button>
            <div class="video-wrapper">
                <!-- Iframe will be used for YouTube links, video tag for local files -->
                <iframe id="modalVideoPlayer" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
                <video id="modalVideoPlayerLocal" controls style="display:none; width:100%; height:auto;"></video>
            </div>
        </div>
    </div>

    <footer class="site-footer">
        <p>© 2025 GA4 Optimizer. All rights reserved.</p>
        <p>
            <a href="/privacy.html">Privacy Policy</a>
            <span>•</span>
            <a href="mailto:support@gaoptimizer.com">Contact Support</a>
        </p>
    </footer>

    <script src="/documentation.js"></script>
</body>
</html>